{
  "name": "backend",
  "version": "1.0.0",
  "description": "Your API lives here. It handles logic the front end shouldn't own (e.g. streaks, weekly summaries, an optional AI insight).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
